# kails-beauty
A simple static webpage built using HTML, CSS, and JavaScript is a basic website that displays fixed content and runs directly in the browser without requiring a server-side language or database.
